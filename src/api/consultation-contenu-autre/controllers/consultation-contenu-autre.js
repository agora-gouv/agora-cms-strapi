'use strict';

/**
 * consultation-contenu-autre controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::consultation-contenu-autre.consultation-contenu-autre');
